import block from 'virtual:icons/material-symbols/block';
import call from 'virtual:icons/material-symbols/call';

export const Filled = {
  Call: call as string,
  Block: block as string,
} as const;
