# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build persianpart-api-2`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t persianpart-api-2`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system persianpart-api-2 && \
          adduser --system -G persianpart-api-2 persianpart-api-2

COPY dist/apps/persianpart-api-2 persianpart-api-2
RUN chown -R persianpart-api-2:persianpart-api-2 .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix persianpart-api-2 --omit=dev -f install

CMD [ "node", "persianpart-api-2" ]
